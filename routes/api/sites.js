const express = require("express");
const router = express.Router();
const sitesCtrl = require("../../controllers/sites");

//all routes are prepended with "/api/sites"
/*---------- Public Routes ----------*/

/*---------- Protected Routes ----------*/

module.exports = router;
